{# purpose of this template: reusable display of entity categories #}
{% if obj is defined %}
    <div class="alert alert-info blogger-relevant-articles">
    <span class="blogger-info-icon"><i class="fa fa-info-circle fa-2x" aria-hidden="true"></i></span>
    <h3>{{ __('Other articles in this series of articles') }}</h3><br style="clear: both;" />
    {% for post in obj %}
        <div class="blogger-relevant-title">{{ post.title }}</div>
        <div class="blogger-relevant-summaryofpost">{% if post.textForRelevant != '' %}{{ post.textForRelevant }}{% else %}{{ post.summaryOfPost|striptags|truncate(100, true, '...') }}{% endif %} <a href="{{ path('mubloggingmodule_post_display', { 'slug': post.slug })}}">{{ __('More') }}</a></div>
    {% endfor %}
    </div>
{% endif %}
