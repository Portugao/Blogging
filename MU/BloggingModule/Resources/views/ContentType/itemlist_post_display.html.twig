{# Purpose of this template: Display posts within an external context #}
{% for post in items %}
    <h3>{{ post.getTitleFromDisplayPattern() }}</h3>
    <p><a href="{{ path('mubloggingmodule_post_display', { 'id': post.id, 'slug': post.slug }) }}">{{ __('Read more') }}</a>
    </p>
{% endfor %}
